{
  "name": "crack-hack",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "worker1": "tsc && set PORT=3001 && set MANAGER_URL=http://localhost:3000 && node dist/worker/main.js",
    "worker2": "tsc && set PORT=3002 && set MANAGER_URL=http://localhost:3000 && node dist/worker/main.js",
    "worker3": "tsc && set PORT=3003 && set MANAGER_URL=http://localhost:3000 && node dist/worker/main.js",
    "workers": "concurrently \"npm run worker1\" \"npm run worker2\" \"npm run worker3\"",
    "manager": "tsc && set WORKER_URLS=http://localhost:3001,http://localhost:3002,http://localhost:3003 && node dist/manager/main.js",
    "dev": "concurrently \"npm run workers\" \"npm run manager\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.6",
    "express": "^5.2.1",
    "fast-xml-parser": "^5.4.1",
    "md5": "^2.3.0",
    "queue-typed": "^2.4.3",
    "xmlbuilder2": "^4.0.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "concurrently": "^9.2.1",
    "tslint": "^6.1.3",
    "typescript": "^5.9.3"
  }
}
